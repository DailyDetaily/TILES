# 검증 기록

검증 날짜: 2026-09-09. 환경: macOS 26.0, Intel x86_64, Swift 6.3.3. 최소 배포 대상 macOS 14는 설정값이며 macOS 14 실기기 검증 결과가 아닙니다.

## 자동 검증

- OrganizerCore XCTest: **25개, 실패 0개**. 60.4초. `test-results.log`.
- CLI 통합 검증: **PASS**. 임시 항목 7개 중 3개 이동 후 새 Organizer 인스턴스에서 3개 복구. 복구한 자료의 SHA-256 내용과 inode가 원본 스냅샷과 일치. `smoke-results.json`.
- 포함한 주요 조건: 원본/프로젝트/숨김/링크 보호, 분류 필요 항목 직접 지정, 목적지 충돌, 같은 크기로 바뀐 파일 내용, 신규 코드·문서 참조, 조사 한도 초과, 중단 뒤 복구, 되돌리기 경로 충돌, 이동 후 편집된 자료 보호, 실행 기록 폴더 교체, 선택 범위 이탈, 원본 폴더 링크 교체, 새 프로젝트 마커, 새 파일이 있는 생성 폴더 보존.
- Release 빌드와 앱 로컬 서명 검증 성공. `build-results.log`.

## 실제 앱 조작

별도의 임시 폴더와 데모 전용 기록을 사용했습니다.

- 분석 결과: 정리 가능 3, 유지 2, 분류 필요 1, 제외 1. 기본 선택 0개.
- 추천 항목 선택으로 3개 선택, 실행 확인 창의 원본·목적지 목록 표시.
- UI의 실행 버튼으로 3개 이동. 기록에 `완료`와 3개의 이동 항목 표시.
- UI의 되돌리기 확인 창에서 복구 실행. 기록에 `되돌림 완료`, 재실행 버튼 비활성 표시.
- 별도 파일 시스템 검사에서 3개의 원본 SHA-256과 inode 일치, 목적지 항목 없음 확인. `ui-run-check.json`.
- 규칙 화면 표시 및 기존 규칙 저장 알림 확인. 분류 메뉴 표시 확인. 이 검증에서 규칙 텍스트 편집 성공을 주장하지 않습니다.
- 퍼즐 겹침 및 초기 목록 순서를 실제 화면을 보고 보정했습니다.

## 경계

테스트를 위해 실제 사용자 자료를 이동하지 않았습니다. 참조 조사는 선택한 범위의 지원 텍스트에 한정됩니다. 외장 드라이브 간 이동, iCloud 미다운로드 파일, 임의 앱의 내부 링크, macOS 14 실기기, Apple Silicon 네이티브 실행, 배포 공증은 검증하지 않았습니다. 코드의 동작 줄이기 분기는 포함되어 있으나 시스템 설정을 변경한 실기기 모션 검증은 하지 않았습니다.


## 벤토 그리드 변경 후 검증

- 앱 전체 6열 × 4행 정사각형 격자, 10pt 간격, 한 칸 4분할 집계와 여러 칸 연결 카드로 변경했습니다. 디자인 그래픽에 한정하지 않고 실제 레이아웃에 적용했습니다.
- 변경한 Release 앱에서 임시 자료 7개를 분석하고 기본 0개 선택 → 추천 3개 선택 → 확인 창 → 3개 이동 → 기록의 완료 상태 → 되돌리기 확인 → 되돌림 완료를 검증했습니다.
- `bento-ui-check.json`: 원본 7개 모두 SHA-256과 inode 일치, 기록된 3개 이동 모두 undone, 생성한 목적지 폴더가 비어 제거된 상태. 실제 사용자 자료는 테스트로 이동하지 않았습니다.
- 1280×840 및 1080×720 창에서 실제 표시를 확인했습니다. 작은 창의 제목·버튼 줄바꿈과 넘침을 보정했습니다. 규칙 화면 입력·저장 버튼이 표시되고 긴 내용은 카드 안에서 스크롤됩니다.
- 엔진 소스는 이 디자인 변경에서 수정하지 않았으므로 앞선 25개 테스트 결과를 유지합니다. 새 디자인만을 이유로 같은 엔진 테스트를 반복 실행하지 않았습니다.
- `bento-build-results.log`에 최종 Release 빌드와 로컬 코드 서명 검증을 기록합니다. 동작 줄이기 분기는 코드에 적용했으며 시스템 설정을 바꿔 검증한 결과는 아닙니다.

- 디자인 캔버스 v7은 같은 격자 구조와 4분할, 큰 한국어 타이포그래피를 적용한 시연 화면입니다. 실제 파일 접근과 이동은 네이티브 앱에서만 수행합니다.

- 최종 Release 앱을 데모 인수 없이 실행했습니다. 소스 폴더가 비어 있는 첫 화면, 기본 도착 위치 `~/Documents/자료`, 폴더 선택 전 분석 버튼 비활성 상태를 확인했습니다.

## 슬라이딩 퍼즐 모션 변경 후 검증 — 2026-09-10

- `OrganizerMotion`의 XCTest **4개, 실패 0개**, 테스트 0.370초, 빌드 18.78초. 중간 배치와 이동 구간의 카드 겹침, 한 축 한 칸 이동, 탭 전환의 4회 이동과 600ms 상한, 모든 중간 착지점에서 최신 목적지로 재계산, 동일 상태의 무동작을 검사했습니다. 성공한 실행 출력의 발췌는 `motion-test-pass.log`입니다. `motion-test-results.log`는 앞서 샌드박스 캐시 쓰기 권한 때문에 실패한 실행 기록이며 성공 증거가 아닙니다.
- 마지막 카운터 변경까지 포함한 Release 빌드 성공(23.45초), 로컬 코드 서명 검증 성공. 파일 이동 엔진은 수정하지 않았고 앞선 엔진 테스트 25개를 이번 모션 작업에서 반복 실행하지 않았습니다.
- 최종 앱을 데모 인수 없이 실행하여 1080×720 창에서 정리 → 기록 → 규칙 → 정리 전환 후 실제 배치를 확인했습니다. 총계·집계·동작 카드가 해당 화면의 위치에 도착했고, 정리 화면 복귀 시 안내 카드가 세로 3칸으로 돌아왔습니다. 소스는 빈 상태이며 분석 버튼은 비활성입니다.
- 웹 시연에서 기록 화면 전환, 새 정리로 복귀, 분석 중 상태, 결과 카드 확장을 확인했습니다. 해당 경로에서 수집된 브라우저 오류 로그는 없었습니다. 숫자 교체 후 집계 숫자 크기가 줄어드는 CSS 충돌도 보정했습니다.
- 자동 검사는 순수 경로 계산에 대한 결과입니다. 화면 캡처는 전환 후 배치를 검증하며 프레임 성능·실제 90ms 측정·OS 동작 줄이기 설정 검증을 의미하지 않습니다. 이번 모션 확인에서 실제 사용자 자료를 이동하지 않았습니다.

- 최종 디자인 캔버스 저장 버전: **v11**, 동일 draft의 저장 HTML에서 빈칸 경로, 숫자 스타일 보정, 선택 수 교체 동작을 확인했습니다.

## 프로젝트 정리·여러 파일·감시 — 2026-09-11

이번 검증은 임시 테스트 폴더에서 수행했습니다. 실제 바탕화면이나 사용자 작업 파일을 이동하거나 감시 대상으로 등록하지 않았습니다. 캡처·원본 경로·실행 로그는 로컬 `verification/project-organizer-20260911/`에 보관하며 Git에는 포함하지 않습니다.

- 통합 후 전체 XCTest 137개가 실패 없이 통과했습니다(365.099초). 이후 권한 변경, 저장 실패 복구, 대기 시간 변경, 500개 감시 대기열, Finder 드래그 연산 처리를 추가하고 관련 회귀 테스트 73개를 다시 실행해 모두 통과했습니다(139.342초). 두 실행에서 통과한 서로 다른 테스트는 146개입니다. 마지막 행 표시 갱신 수정은 이 회귀 실행 이후 릴리스 빌드와 실제 UI로 확인했습니다.
- 테스트는 여러 파일을 하나의 기록으로 이동·되돌리기, 폴더만 생성·되돌리기, 폴더 충돌과 원본 변경 시 실행 전 중단, 읽기 전용 원본·목적지 차단, 기존 기록 호환성, 프로젝트 분류의 모호함·미확인 보류, 실제 로컬 이미지/스캔 PDF OCR, 대기열 저장 실패 시 메모리·디스크 복구, 감시의 시간·안정성·재시작·중복 전달 억제를 포함합니다.
- 기존 `OrganizerSmoke`도 임시 파일 7개 분석 → 3개 이동 → 3개 복구로 통과했습니다. 이 결과는 기존 폴더 전체 정리 경로의 확인이며 새 UI 검증과 구분합니다.
- 네이티브 앱에서 파일 3개 선택 → 새 프로젝트 → 기본 템플릿 선택 → “결과물을 웹용과 인쇄용으로 나눠줘” 적용 → 폴더 생성/이동 미리보기 → 실행 → 되돌리기를 확인했습니다. 미리보기 단계에서는 실제 폴더가 생기지 않았습니다. 실행에서 파일 3개 이동과 폴더 6개 생성이 하나의 기록에 저장됐으며, 되돌린 뒤 SHA-256·inode가 모두 원본과 같고 이번에 생성한 폴더만 제거됐습니다. 기존 목적지 폴더는 유지됐습니다.
- Release 앱의 Finder 드래그에서 3개 파일을 ‘정리 추천’에 놓으면 드래그 종료 후 별도 검토 창이 열렸습니다. 기존 감시 대기 파일의 프로젝트·하위 폴더 선택은 보존되고 새 파일 3개가 추가됐습니다. 이 단계에서 원본 3개의 SHA-256·inode는 바뀌지 않았습니다. Finder가 실제 제공한 drag operation mask `4`에 대한 경로도 진단 로그로 확인했습니다.
- ‘바로 이동’에 파일 2개를 놓아 단일 기록으로 이동하고, 기록 화면의 확인 창에서 되돌렸습니다. 최종적으로 임시 원본 6개 모두 최초 SHA-256·inode와 같았으며 두 실행 기록은 `undone`입니다. 한 번의 자동화 mouse-up 전달 누락은 OS 버튼 상태와 Finder의 `dragUntilMouseUp` 샘플로 확인한 뒤 실제 버튼을 해제해 정상 완료했습니다.
- 폴더 감시는 초기 꺼짐과 기본 10분을 확인했습니다. 직접 선택한 임시 폴더에서 이미 대기 시간이 지난 파일 하나를 45초 간격의 두 관찰로 안정성 확인하고 검토 대기열에 한 번 전달했습니다. 여러 차례 재확인해도 중복 배치가 생기지 않았고 원본은 유지됐습니다. 확인 후 감시를 다시 껐습니다. OS 알림 허용·배너 표시는 이번 실기 검증에 포함하지 않습니다.
- 실제 UI에서 프로젝트·하위 폴더 일괄 지정 후 개별 행의 메뉴와 준비 상태가 함께 갱신되는 것을 확인했습니다. 1080×720 메인 창에서 파일 행이 두 줄 구성으로 바뀌고 목록이 카드 안에서 스크롤되며, 기록·되돌리기 버튼이 표시됩니다. 별도 검토 창은 닫아도 대기열과 선택을 보존합니다.
- 마지막 변경은 숨겨진 Dock 도우미의 응답을 위해 활성 기간에만 활동 토큰을 유지하고 비활성/종료 시 해제하는 처리입니다. 시스템·화면 유휴 절전은 허용합니다. 관련 `OverlayTransportTests` 8개가 추가 실행에서 통과했고, 해당 Release 빌드(177.73초)와 ad hoc 코드 서명 검증이 성공했습니다. 최초 지연의 원인이 App Nap이었다고 확정한 결과는 아닙니다.
- 앱 재시작 뒤 같은 파일을 다시 넣는 추가 회귀를 보완했습니다. 전체 대기열에서 NFC/NFD 경로 표기 차이를 포함해 직접 접수와 감시 접수의 중복을 막고, 기존 파일만 다시 넣으면 관련 묶음의 선택을 보존합니다. 마지막 모델에서 Flow 9개·Persistence 3개, 총 12개가 통과했습니다(42.608초). 이번 작업에서 통과한 서로 다른 테스트는 총 147개입니다.
- 마지막 모델 변경을 포함한 Release 빌드와 코드 서명 검증도 성공했습니다(181.67초). 알림 배너, 다중 모니터·Spaces 전체 조합, 장시간 실행의 성능은 이번 결과로 확정하지 않습니다.
- 마지막 Release 앱을 재실행하고 Finder에서 이미 대기 중인 파일을 다시 드롭했습니다. 대기 수와 묶음 ID가 증가하지 않고 관련 기존 묶음이 열렸으며, 기존 프로젝트·하위 폴더·포함 여부와 원본이 유지됐습니다. 테스트용 QA 앱과 도우미는 검증 후 종료했습니다.
