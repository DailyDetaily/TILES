# 슬라이딩 퍼즐 모션

글자 사이 간격은 빈 조각 1칸이다. 조각 5pt·피치 6pt 기준 인접 글자의 바깥 타일 사이 실제 여백은 7pt다. 기본 TILES의 글자 폭은 19칸(113pt)이며, 모션 작업 격자는 기존 23칸 폭을 유지한다.

## 불규칙한 퍼즐 순서 — 2026-09-10

왼쪽 위부터 행·열 순서로 지우거나 채우지 않는다. 가까운 조각과 빈칸을 짝지으면서, 방금 처리한 곳에서 떨어진 후보를 골라 여러 구역을 오가며 맞춘다. 같은 거리의 후보와 최단 경로의 가로·세로 순서를 섞고, 한 번 더 이동하는 범위까지 후보를 허용해 움직임을 분산한다. 완성 윤곽 밖 조각의 뒤집기도 이동 사이에 끼워 넣는다.

앱은 전환마다 새로운 시드를 사용한다. 검증에서는 같은 시드로 같은 경로를 재현할 수 있다. 인접 빈칸으로 한 칸씩 이동하는 규칙, 조각 수 차이만큼만 뒤집는 규칙, 새 조각이 격자 안 비목표 빈칸에서 뒤집힌 뒤 이동하는 규칙은 유지한다. 검증과 현재 글자 모양을 반영한 영상은 verification/irregular-puzzle/에 기록한다.

## 특수문자 — 2026-09-10

느낌표·물음표·문장부호·연산 기호·괄호를 포함한 ASCII 특수문자 32개와 …, ×, ÷를 타일로 지원한다. 영문·숫자와 같은 6행 격자와 5pt 정사각형 조각을 쓰며, 기호 사이와 문자·숫자 사이 모두 기존 슬라이딩/상하 원근 뒤집기 규칙을 따른다. 점은 밑선에 놓고, 느낌표·물음표는 몸통과 점 사이 빈 행을 유지한다.

소문자는 대문자로, 곡선 따옴표는 직선 따옴표로, 긴 대시·빼기 기호는 기존 대시로, 전각 느낌표·물음표는 같은 타일 기호로 표시한다. 한글 문장·버튼 속 문장부호는 일반 폰트를 유지한다. 완료 표시는 고정 23열 헤더 안에 맞는 DONE!을 사용하고, 완료/되돌림 완료일 때만 표시한다.

35개 특수문자와 영문·숫자를 합친 71개 글리프의 5,041개 전환, 혼합 문구의 중간 전환, 기호 모양은 verification/puzzle-symbols/에서 검증한다.

## 화면 숫자 — 2026-09-10

전체·분류별 집계와 독립적인 안내 단계 번호만 PuzzleCounter로 표시한다. 버튼, 탭, 작은 섹션 라벨, 선택 수와 한글이 이어지는 문구, 확인 제목, 기록 항목 수는 기존 일반 숫자 텍스트를 사용한다. 기존 타일 문자와 같은 숫자 윤곽 및 슬라이딩/상하 원근 뒤집기를 사용한다. 흰 숫자/검은 카드, 검은 숫자/파란·회색 카드, 회색 단계 번호의 대비를 유지한다.

자릿수가 커지면 기존 조각을 보존하며 보드 열 수만 확장한다. 경로 전체를 포함하는 표시 폭을 전환 중 유지하고, 착지한 뒤 현재 숫자 폭으로 맞춘다. 좁은 카드에서는 전체 수치를 유지한 채 같은 비율로 축소한다. 미집계 대시와 0은 별도 타일 모양으로 표현한다. 접근성 레이블에는 원래 숫자 값을 제공한다.

검증 결과는 verification/tile-numbers/VERIFICATION.md에 기록한다.

타일 0은 3×5칸으로, 010 / 101 / 101 / 101 / 010을 사용한다. 다른 숫자와 높이·밑선을 맞추며 일반 텍스트의 0은 변경하지 않는다. 6행 작업 격자는 키가 큰 특수문자와 모션을 위해 유지한다.

## 상단 퍼즐 문자 — 2026-09-10

앱 이름과 기본 워드마크는 TILES다. 정사각형 조각으로 A–Z와 0–9를 조합하며, 기본 TILES 윤곽은 45개 조각이다. S는 사용자 지정 111 / 100 / 111 / 001 / 111 패턴이다. 조각은 5pt, 간격은 1pt이며, TILES·HELLO·READY를 수용하는 고정 작업 격자는 23×6칸(137×35pt)이다. 기존 5행 문자의 화면 위치를 보정해 유지한다. 헤더 높이는 64pt를 유지한다.

| 결정 | 근거 | 적용 |
| --- | --- | --- |
| 슬라이딩과 뒤집기 조합 | 사용자가 선택한 흰색·검정 뒤집기 | 위치 차이는 이동, 검은 면 개수 차이만 뒤집기로 처리한다. |
| 조각 수가 같으면 기존 조각만 이동 | 사용자 후속 확인 | 뒤집기 없이 모든 기존 조각의 ID를 유지하며 다음 글자에 재사용한다. |
| 새 조각은 격자 안의 비목표 빈칸에서 시작 | 사용자 후속 지시 | 같은 고정 격자 안에서 완성 글자에 포함되지 않는 빈칸을 골라 흰 면을 검게 뒤집고 필요한 칸으로 이동한다. 안쪽 목표에 필요하면 기존 조각이 빈칸을 전달해 길을 만든다. 목표 자리에서 바로 검게 뒤집지 않는다. |
| 남는 검은 면은 흰 면으로 복귀 | 글자별 조각 수 차이 | 완성 윤곽 밖의 남는 조각만 뒤집으며, 필요한 위치 이동 사이에 순서를 섞는다. |
| 한 칸, 한 축, 빈칸 | 슬라이딩 퍼즐 규칙 | 검은 정사각형은 한 번에 한 칸만 수평·수직 이동한다. 이동 중 다른 검은 조각을 관통하지 않는다. |
| 정사각형 앞뒷면 | 사용자의 뒤집기 제안 | 뒤집기 동안 가로 회전축을 중심으로 위아래로 뒤집으며, 가까운 모서리가 커지고 먼 모서리가 작아지는 원근 투영과 명암을 적용하고 90도에서 흰색·검정을 바꾼다. 이동 시 조각 크기는 고정한다. |
| 메시지는 실제 작업 상태를 따른다 | 인사·완료·안내 요청 | 첫 표시·대기 상태 재활성화·호버는 HELLO, 처리 중 WAIT, 분석 완료 READY, 완료·되돌림 완료 DONE!, 중단 STOP, 오류·미완료 CHECK다. |
| 읽은 뒤 TILES로 복귀 | 유한 재생 | HELLO는 도착 후 1.8초, DONE!·STOP은 3초 유지한다. WAIT·READY·CHECK는 상태가 바뀔 때까지 유지한다. |
| 최신 상태가 우선 | 중간 전환 취소 | 현재 한 칸 또는 뒤집기를 마친 다음 최신 메시지로 경로를 다시 계산한다. 이전 메시지의 예약 복귀는 취소한다. |
| 동작 줄이기·비활성 | 기존 접근성·성능 기준 | 동작 줄이기에서는 완성 글자로 즉시 전환한다. 비활성·화면 종료 때 진행 중 모션을 마무리하고 재생을 중단한다. |

한 칸 이동은 36ms, 뒤집기는 120ms다. 전체 길이는 필요한 이동과 조각 수에 따라 달라진다. 모든 상태에서 글자를 계속 섞는 반복 애니메이션은 없다.

PuzzleAlphabet은 영문·숫자 36개, 특수문자 35개와 공백을 지원하고 소문자 ASCII를 대문자로 표시한다. 지원하지 않는 문자·빈 문자열·보드보다 긴 문구는 명시적으로 거부한다. 더 긴 문구는 보드 열 수를 지정할 수 있다. TileWordmarkMotion은 경로를 만들고, TileWordmarkPlayer는 중단·재계산·복귀를 관리하며, TileWordmarkFace는 실제 조각을 그린다.

참고는 사용자 제공 영상과 현재 앱 디자인, Refero 번들 motion.md다. 라이브 Refero 검색은 NO_SUBSCRIPTION 응답으로 이용할 수 없었다. 영상 원본은 수정하지 않았다. 검증 결과와 재현 자료는 verification/puzzle-messages/VERIFICATION.md에 기록한다.

---

대상은 기존 자료 정리 Mac 앱과 동일한 디자인 미리보기다. 6×4 정사각형 그리드, 10pt 간격, 4분할 집계, #4c92e9 악센트, 글꼴과 정리 동작을 유지한다.

## 기준과 결정

| 결정 | 근거 | 적용 |
| --- | --- | --- |
| 빈칸으로 인접 타일이 들어온다 | 사용자 지정 슬라이딩 퍼즐 모티브 | 서로의 자리를 관통하는 맞교환을 없앤다. |
| 한 번에 한 축, 한 칸 | 사용자 지정 정사각형 격자 | 수평 또는 수직 이동만 허용한다. 대각선·회전·튀는 탄성은 쓰지 않는다. |
| 새 내용이 열릴 위치를 먼저 확보한다 | Refero motion.md의 continuity·hierarchy | 본문을 접어 빈칸을 만든 뒤 상단 집계와 동작 카드를 순서대로 이동한다. |
| 큰 전환과 작은 반응에 다른 리듬을 쓴다 | Refero motion.md의 빈도별 duration·interruptibility | 한 칸 이동은 약 90ms, 확장·정착은 120ms, 반복 입력은 100ms 이내다. 복합 경로만 여러 박자를 연결한다. |
| 원본 행은 검토 중 고정한다 | 기존 제품 흐름과 사용자 자료 보호 | 체크박스·검색 입력 때문에 파일 행이나 그리드 전체를 섞지 않는다. |
| 새로운 요청이 최신 목적지가 된다 | Refero motion.md의 interruptible motion | 현재 한 칸 이동을 마친 즉시 최신 탭으로 경로를 다시 계산한다. 오래된 전환은 실행하지 않는다. |
| 동작 줄이기는 최종 상태로 바로 간다 | Refero motion.md·기존 accessibilityReduceMotion | 이동·확장·카운터 슬라이드를 생략한다. |

라이브 Refero MCP 도구는 현재 연결되어 있지 않아, 이미 승인된 앱 디자인과 스킬에 포함된 Motion & Micro-interactions Guide를 사용했다. 외부 디자인을 새로 검색하거나 다른 스타일로 바꾸지 않는다.

## 화면과 상태별 동작

- 정리 ↔ 기록 ↔ 규칙: 본문 오른쪽 한 열을 잠시 비워 2×2 타일 경로를 확보한다. 총계·4분할 집계·동작 카드가 빈칸을 따라 차례로 이동한다. 빈칸이 본문 자리로 돌아오면 본문이 다시 펼쳐진다. 반대 방향 전환은 같은 경로를 역으로 밟는다.
- 미리보기 완성: 안내 카드를 먼저 두 칸 높이로 접고, 아래로 한 칸 → 옆으로 한 칸 이동한다. 비워진 열로 결과 카드가 확장된다.
- 다시 분석/선택 폴더 변경: 결과 영역이 접힌 뒤 안내 카드가 역순으로 원래 자리로 돌아간다. 분석을 기다리는 동안 타일을 무한 반복하지 않는다.
- 선택 수·집계 숫자: 같은 카드 영역에서 타일 숫자로 전환한다. 기존 조각을 재사용하고 필요한 차이만 격자 안에서 뒤집어 보충하거나 비운다.
- 실제 이동 확인과 되돌리기 확인: 네이티브 확인 창과 실행 절차는 그대로 사용한다. 파일 동작은 모션 완료와 연결하지 않는다.

## 검증할 조건

모든 중간 배치가 그리드 안에 있고, 카드끼리 겹치지 않으며, 이동이 인접한 빈칸으로 향하는지 확인한다. 정방향·역방향·연속 탭 입력·분석 결과 전환·동작 줄이기를 검사한다. 실제 파일 이동 테스트 대신 임시 예시 자료를 사용한다.
